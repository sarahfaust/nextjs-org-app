exports.up = async function (sql) {
  console.log("Creating table 'winddowns'...");
  await sql`
	CREATE TABLE winddowns (
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		day_id integer NOT NULL,
		review varchar(500),
		planning varchar(500),
		grateful varchar(500),
		keep_habit varchar(500),
		improve_habit varchar(500),
		CONSTRAINT fk_days
		FOREIGN KEY (day_id)
		REFERENCES days(id)
	);`;
};

exports.down = async function (sql) {
  console.log("Deleting table 'winddowns'...");
  await sql`
		DROP TABLE winddowns;
	`;
};

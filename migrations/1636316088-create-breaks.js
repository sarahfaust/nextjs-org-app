exports.up = async function (sql) {
  console.log("Creating table 'breaks'...");
  await sql`
	CREATE TABLE breaks (
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		profile_id integer NOT NULL,
		incentive_type_id integer,
		incentive_id integer,
		CONSTRAINT fk_profiles
		FOREIGN KEY (profile_id)
		REFERENCES profiles(id),
		CONSTRAINT fk_incentive_types
		FOREIGN KEY (incentive_type_id)
		REFERENCES incentive_types(id),
		CONSTRAINT fk_incentives
		FOREIGN KEY (incentive_id)
		REFERENCES incentives(id)
	);`;
};

exports.down = async function (sql) {
  console.log("Deleting table 'breaks'...");
  await sql`
		DROP TABLE breaks;
	`;
};

exports.up = async function (sql) {
  console.log("Creating table 'winddown'...");
  await sql`
	CREATE TABLE winddown (
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		day_id integer NOT NULL,
		review varchar(500),
		planning varchar(500),
		grateful varchar(500),
		"keep" varchar(500),
		improve varchar(500),
		CONSTRAINT fk_day
		FOREIGN KEY (day_id)
		REFERENCES day(id)
	);`;
};

exports.down = async function (sql) {
  console.log("Deleting table 'winddown'...");
  await sql`
		DROP TABLE winddown;
	`;
};
